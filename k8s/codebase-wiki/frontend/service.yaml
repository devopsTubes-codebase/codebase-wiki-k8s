apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: codebase-wiki
  labels:
    app: frontend
    app.kubernetes.io/part-of: codebase-wiki
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  type: NodePort
